[project]
name = "taskmanager"
version = "0.1.0"
description = "A simple task management API built with FastAPI and SQLAlchemy."
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "fastapi",
    "sqlalchemy",
    "uvicorn[standard]",
]
